# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

platform :ios, '13.0'

target 'MeekoUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MeekoUI
  pod 'Alamofire', '~> 4.7.3'
  pod 'PromiseKit/Alamofire', '~> 6.0'
  pod "PromiseKit", "~> 6.8"
  pod 'KeychainSwift', '~> 13.0'
  pod 'SwiftSoup', '~> 1.7'
  target 'MeekoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MeekoUIUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
